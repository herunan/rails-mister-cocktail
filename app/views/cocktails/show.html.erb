<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path @cocktail.photo.key, :height => 1000, :crop => :fill if @cocktail.photo.attached? %>);">
  <div class="container">
    <h1>How to make a <strong><%= @cocktail.name %></strong></h1>
    <p><%= @cocktail.description %></p>
  </div>
</div>
<div class="container">
  <div class="row justify-content-between">
    <div class="col-12 col-md-6">
      <h2>Method</h2>
      <p><%= simple_format @cocktail.method %></p>
      <%= link_to '<i class="fa fa-arrow-left"></i> Back to list of cocktails'.html_safe, root_path, html_options = { :class => "btn btn-primary" } %>
    </div>
    <div id="ingredients" class="col-12 col-md-5">
      <h2>Ingredients</h2>
      <% if @cocktail.doses.blank? %>
        <p>No ingredients have been added for <%= @cocktail.name %></p>
      <% else %>
        <ul>
          <% @cocktail.doses.each do |dose| %>
            <%= render 'doses/show', dose: dose %>
          <% end %>
        </ul>
      <% end %>
      <%= render 'doses/form', coktail: @cocktail, dose: @dose %>
    </div>
  </div>
</div>
