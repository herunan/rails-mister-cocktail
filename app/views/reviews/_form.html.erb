<%= simple_form_for [ cocktail, review ] do |f| %>
  <%= f.input :rating, collection: 0..5, prompt: 'Number of stars', label: false %>
  <%= f.input :content, as: :text, placeholder: "Write your review", label: false %>
  <%= f.submit "Post review", class: "btn btn-primary" %>
<% end %>
